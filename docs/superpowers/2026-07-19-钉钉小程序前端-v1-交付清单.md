# 钉钉小程序前端 v1 · 交付清单

> 2026-07-19。代码侧(Task 1–4)已实现并入 `main`；本清单＝**文件盘点 + 部署/测试勾选表**。
> 计划：`docs/superpowers/plans/2026-07-19-钉钉小程序前端-v1.md`；设计：`docs/superpowers/specs/2026-07-19-钉钉小程序前端-design.md`。
> 账号/密钥怎么办：`docs/superpowers/2026-07-19-智能实勘问卷-二期账号与部署指南.md`；broker 部署校准细节：`serverless/survey_broker/README.md`。

---

## 1. 状态总览

| 项 | 状态 |
|---|---|
| Task 1 broker `whoami`(免登) | ✅ 代码 + **已重传 FC 并真机验通** |
| Task 2 小程序骨架 + broker 封装 | ✅ 已提交(`38191ca`) |
| Task 3 入口页(免登+草稿+新建) | ✅ 已提交(`bf58b9f`) |
| Task 4 表单页(19字段+地图+暂存/提交) | ✅ 已提交(`ffeb65c`) |
| Task 5 部署 + 真机门 | 🔄 小程序已上线✅/broker含whoami已传✅/免登真机通✅/地图通✅;**余表单暂存·提交·办公端拉取** |
| 自动化验证 | ✅ 全量 622 测试绿、`ruff`/`mypy` 净 |
| whoami 免登端点 | ✅ **2026-07-19 真机验通**(`topapi/v2/user/getuserinfo` 一次命中,零校准) |

**v1 范围**：19 基本信息 + 地图预填 + 免登 + 暂存/本机续填/提交。**不含**：28 档次、逐因素描述、照片(v2)。

---

## 2. 文件清单(本次 v1 新增/改动)

### 2.1 broker + 后端(Python，随 `28a4b74`)
| 文件 | 行 | 说明 |
|---|---|---|
| `serverless/survey_broker/identity.py` | 71 | **新增** `DingtalkIdentity`：authCode→{userid,name}，复用同应用 access_token，transport 可注入 |
| `serverless/survey_broker/handler.py` | 158 | 改：`_Identity` 协议 + `dispatch` 增 `identity` 参与 `whoami` 路由 + `handler()` 构造 identity |
| `serverless/survey_broker/server.py` | 115 | 改：`build_context`/`handle`/`_make_handler`/`main` 全程带 identity |
| `src/dingtalk/notable.py` | 228 | 改：暴露 public `access_token()`(薄包 `_access_token`) |
| `serverless/package.py` | 50 | 改：`FILES` 补 `identity.py`(否则打包漏文件) |
| `tests/test_survey_broker_identity.py` | 34 | **新增** whoami 单测(3 例) |
| `tests/test_survey_broker_handler.py` | 220 | 改：`FakeIdentity` + 全 `dispatch` 补 `identity=` + whoami 路由测试 |
| `tests/test_survey_broker_server.py` | 106 | 改：`FakeIdentity` + 全 `handle` 补 `identity=` + whoami 端到端测试 |

### 2.2 钉钉小程序 `miniprogram/`(12 文件，随 `38191ca`/`bf58b9f`/`ffeb65c`)
```
miniprogram/
├── app.json                8   页面注册 + window
├── app.js                  7   globalData: BASE_URL / filler / fillerName
├── app.acss                4   全局样式
├── utils/broker.js        22   dd.httpRequest 封装 request(action,payload)→Promise
├── pages/index/
│   ├── index.axml         17   填报人 + 新建 + 本机草稿列表
│   ├── index.js           42   getAuthCode→whoami 免登 + loadMyDrafts + 新建/续填跳转
│   ├── index.acss          1
│   └── index.json          1
└── pages/form/
    ├── form.axml          32   类别 picker + 19 字段 + 地图预填卡 + 暂存/提交
    ├── form.js           111   FIELDS/CATEGORIES + resume/onGeo/saveDraft/submit + 本机 myDrafts
    ├── form.acss           3
    └── form.json           1
```

### 2.3 部署产物(gitignored，不进库)
- `serverless/dist/survey_broker_fc.zip` — FC 上传包，**11 文件、零第三方依赖**(已含 `identity.py`)。重跑 `uv run python serverless/package.py` 可再生。

---

## 3. 部署 + 测试勾选表

> broker 上一轮已上线并真机验通，本次只是**换一个带 whoami 的新代码包 + 传小程序**。
> 线上 URL：`https://zhenghee-report-bmpupipvct.cn-hangzhou.fcapp.run`(华东杭州 FC，认证暂 anonymous)。

### 第 1 步 · 重传 broker(不用改任何环境变量)
- [ ] `cd 估价报告系统 && uv run python serverless/package.py`(生成最新 zip)
- [x] 阿里云 FC 控制台 → 现有函数 → 上传 `serverless/dist/survey_broker_fc.zip` → 部署 ✅
- [ ] 确认启动命令仍为 `python3 -m serverless.survey_broker.server`、端口 `9000`

### 第 2 步 · 命令行冒烟(不用手机，先确认包上没上)
```bash
BASE=https://zhenghee-report-bmpupipvct.cn-hangzhou.fcapp.run
curl -sS "$BASE/"                                   # 期望 {"ok":true}
curl -sS -X POST "$BASE/" -H 'Content-Type: application/json' \
  -d '{"action":"prefillGeo","payload":{"lng":120.21,"lat":30.25}}'   # 期望地址+周边
curl -sS -w '\n[%{http_code}]\n' -X POST "$BASE/" -H 'Content-Type: application/json' \
  -d '{"action":"whoami","payload":{"authCode":"bogus"}}'
```
- [ ] ①②通(broker 活着)
- [x] ③ 已验:假 code 回 `{"error":"免登失败：不存在的临时授权码"}` [400] ＝ whoami 上线、端点正确 ✅

### 第 3 步 · 钉钉后台 + 上传小程序
- [ ] 钉钉开放平台 → 你的小程序 → **开发配置 → 安全设置**，按下表填(**IP白名单里不要填网址**)：
  | 栏位 | 填什么 |
  |---|---|
  | IP白名单 | **清空/不启用**(只收 IP；FC 出口 IP 不固定，启用反而挡死 broker 调钉钉) |
  | **HTTP 可信域名** | `zhenghee-report-bmpupipvct.cn-hangzhou.fcapp.run`(小程序 `dd.httpRequest` 合法域名←broker 域名放这；要协议就带 `https://`) |
  | 重定向URL(回调域名) | `https://zhenghee-report-bmpupipvct.cn-hangzhou.fcapp.run`(必填项占位；免登走 `dd.getAuthCode` 不实际用它) |
  | 端内免登地址 / Webview 可信域名 | 留空 |
- [x] 安全设置(HTTP 可信域名=broker 域名)已配 + 发版本生效 ✅(手机 httpRequest 打通即证)
- [ ] 权限管理 → 确认已开**免登 / 通讯录个人信息读权限**(whoami 换 userid 靠它)
- [x] **上传小程序代码 → GUI 开发者工具已完成 ✅**(2026-07-19 GUI 导入 miniprogram/、模拟器渲染入口页正常、上传 v1.0.0、版本管理里已上线;CLI 路线因 scope 走死见下)：
  - **⛔ CLI 路线作废**：`ding upload` 硬性要 app scope `qyapi_alipay_eapp_base`(`subcode=60011`),而该 scope 在本应用**权限列表搜不到、`appscope/apply?content=...` 专用链接打开也是空的 → 开不出来**。CLI 以 app token 身份上传,过不了这道门。(CLI 配置 `ding.config.json`+`mini.project.json` 已就绪、lint/打包/鉴权全过,留待将来 scope 若能开或接 CI 复用。)
  - **✅ GUI 路线(唯一可行)**：GUI 以**开发者扫码身份**上传,不吃该 app scope。机器上只有聊天版 `DingTalk.app`,**须另装「钉钉开发者工具」IDE**(下载页 `https://open.dingtalk.com/document/download/develop-mini-program-applications`)→ 扫码登录(管理员/开发者)→ 新建**企业内部应用小程序/E 应用**、MiniAppId `5000000006808190`、目录选 `/Volumes/Elements/workspace/test/估价报告系统/miniprogram` → 编译预览无红错 → 右上**「上传」** 版本号 `1.0.0`。
- [ ] 网页控制台「版本管理与发布」→ 设**体验版** → 加体验成员(你自己)→ 手机钉钉工作台可打开

### 第 4 步 · 真机走全链路(同时校准 whoami)
- [x] 入口页显示「填报人:薛焱」(免登真机通) ✅
- [ ] 新建 → 选类别 → 填字段
- [ ] 点「地图预填地理事实」→ 授权定位 → 回填地址/周边
- [ ] 暂存 → 提示「已暂存:<survey_id>」
- [ ] 返回入口页 → 「本机草稿」列出该条
- [ ] 续填 → 提交 → 提示「已提交，办公端可拉取」

### 第 5 步 · 办公端端到端
- [ ] 办公端 `.env`：`NOTABLE_SURVEY_SHEET=ePwqDH2` + `承载后端=多维表`
- [ ] 出报告页 →「从实勘问卷拉取」→ 见真机提交那份 → 选中 → 预填出表单

---

## 4. whoami 免登端点校准 —— ✅ 已真机验通(2026-07-19),无需再做;以下留作故障时参考
1. FC 控制台 → 该函数 → 日志 → 找 whoami 那次，看钉钉实际回的 errcode/errmsg 或字段名。
2. 把日志给我，我改 `serverless/survey_broker/identity.py` 顶部 `_URL` 与结果解析(`result.userid`/`result.name`)。
3. 重打包(`serverless/package.py`)→ 重传 FC。
> 现按标准免登接口 `https://oapi.dingtalk.com/topapi/v2/user/getuserinfo`(body `{"code":authCode}`，`?access_token=`)写，大概率一次通。

---

## 5. FC 环境变量参考(密钥配进 FC，**绝不进 git**)
| 变量 | 值来源 |
|---|---|
| `DINGTALK_APP_KEY` / `DINGTALK_APP_SECRET` | 钉钉小程序 Client ID / Secret(与承载层同一企业内部应用) |
| `NOTABLE_BASE_ID` / `NOTABLE_OPERATOR_ID` | 多维表 baseId / 操作人 unionId(同承载层，你 `.env` 已有) |
| `NOTABLE_SURVEY_SHEET` | 实勘问卷表 sheetId ＝ `ePwqDH2` |
| `AMAP_KEY` | 高德 **Web 服务** Key |
> whoami **不需要新增变量**，复用上表 `DINGTALK_APP_KEY/SECRET` 换的同一 token。

---

## 6. 回滚
- broker：FC 控制台切回上一个版本；或本地 `git revert 28a4b74` 重打包重传。
- 小程序：钉钉后台版本管理回退体验版；本地文件仅在 `miniprogram/`，删该目录即回到无小程序态。
- 全部改动本地未 push(`main` 领先 origin，私有远端)。
