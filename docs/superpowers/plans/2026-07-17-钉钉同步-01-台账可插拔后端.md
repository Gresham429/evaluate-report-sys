# 钉钉同步 · 第一刀：LedgerStore 可插拔存储后端 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `LedgerStore` 的持久化从"直接读写本地文件"抽到一个**后端接口**后面，本地文件后端**行为一字不变**，并加一个内存后端——为将来"换成钉钉/远端后端"留下干净的插拔点。

**Architecture:** 新增 `LedgerBackend` 协议（append 一条 / 读全部 payload）；把现有文件 I/O 原样搬进 `LocalFileLedgerBackend`；`LedgerStore` 只保留业务逻辑（记录号安全校验、排序、按内容过滤）并委托给后端。默认后端 = 本地文件后端，故所有既有调用点与测试**零改动**。

**Tech Stack:** Python 3.13、typing.Protocol、pytest。

## Global Constraints

- **行为保持**：现有 `tests/test_ledger_store.py`、`tests/test_ledger_replay.py`、`tests/test_web_ledger.py` 等**全部保持全绿**，字节级行为不变（文件名格式、原子写、坏文件跳过、路径穿越拒绝）。
- 铁律 #4 台账只增不改：后端接口**只有 append 与读，没有 update/delete**。
- 铁律 #5 快照自洽：后端搬的是不透明 payload dict，序列化（to_dict/from_dict）仍归 `ledger/model.py`，不进后端。
- 这一刀**只动 LedgerStore**；InstanceStore/BaseTableStore/DraftStore 是后续各自一刀（它们持久化形状不同，尤其 InstanceStore 是整档 load/save）。
- 不做**权限裁剪/同步协议/鉴权**——那些需要"用户身份/远端传输"，是后面配合真后端的刀，现在做即 YAGNI。
- 编码规范：全类型标注、不裸 except、logger 不用 print、中文注释讲为什么。
- 每个任务结束 `uv run pytest && uv run mypy src/ && uv run ruff check .` 全绿再提交。

---

### Task 1: 抽出 LedgerBackend 协议 + LocalFileLedgerBackend（行为不变）

**Files:**
- Create: `src/ledger/backend.py`
- Modify: `src/ledger/store.py`
- Test: 依赖既有 `tests/test_ledger_store.py`（回归护栏）

**Interfaces:**
- Produces:
  - `LedgerBackend`（Protocol）：`append(record_id: str, created_at: datetime, payload: dict[str, object]) -> None`、`iter_payloads() -> Iterator[dict[str, object]]`
  - `LocalFileLedgerBackend(path: Path)`：实现上述，行为=现 LedgerStore 的文件 I/O
  - `LedgerStore(path=DEFAULT_LEDGER_DIR, *, backend: LedgerBackend | None = None)`：backend 为 None 时默认 `LocalFileLedgerBackend(path)`

- [ ] **Step 1: 建 `src/ledger/backend.py`**

```python
"""台账存储后端：把"数据到底落在哪"从 LedgerStore 里分出来。

LedgerStore 管业务（记录号安全、排序、按内容过滤）；后端只管"把一条 payload
存下去、把全部 payload 读出来"。今天只有本地文件后端；将来钉钉/远端后端实现
同一个 Protocol 即可插进来，LedgerStore 一行不改。

**只增不改**（铁律 #4）：接口没有 update/delete。搬的是不透明 payload dict，
序列化仍归 ledger/model.py（铁律 #5 自洽快照不进后端）。
"""

import json
import logging
import os
from collections.abc import Iterator
from datetime import datetime
from pathlib import Path
from typing import Protocol, runtime_checkable

logger = logging.getLogger(__name__)

__all__ = ["LedgerBackend", "LocalFileLedgerBackend"]


@runtime_checkable
class LedgerBackend(Protocol):
    """台账持久化后端。只 append、只读，无 update/delete。"""

    def append(self, record_id: str, created_at: datetime, payload: dict[str, object]) -> None:
        """落一条。record_id 已由 LedgerStore 校验过形状安全。"""//
        ...

    def iter_payloads(self) -> Iterator[dict[str, object]]:
        """逐条读出全部 payload（顺序不保证；排序归 LedgerStore）。"""
        ...


class LocalFileLedgerBackend:
    """一条一个 JSON 文件，原子写，坏文件跳过。行为搬自原 LedgerStore。"""

    def __init__(self, path: Path) -> None:
        self.path = path

    def append(self, record_id: str, created_at: datetime, payload: dict[str, object]) -> None:
        self.path.mkdir(parents=True, exist_ok=True)
        stamp = created_at.strftime("%Y%m%d-%H%M%S")
        file = self.path / f"{stamp}-{record_id}.json"
        text = json.dumps(payload, ensure_ascii=False, indent=2)
        # 先写临时文件再原子替换：中途崩掉不留半份 JSON（台账记下的必须作数）。
        tmp = file.with_suffix(".json.tmp")  # 不叫 *.json，免得被 iter_payloads 扫到
        tmp.write_text(text, encoding="utf-8")
        os.replace(tmp, file)

    def iter_payloads(self) -> Iterator[dict[str, object]]:
        if not self.path.exists():
            logger.debug("台账目录不存在，视为空：%s", self.path)
            return
        for file in self.path.glob("*.json"):
            try:
                yield json.loads(file.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, ValueError):
                # 台账文件明说可手改，改坏就得容错：一份坏文件不连累其余。
                logger.warning("台账文件读不出，已跳过：%s", file, exc_info=True)
```

（注意：把上面 `...` 前那个笔误 `//` 删掉——协议方法体只留 docstring + `...`。）

- [ ] **Step 2: 改 `src/ledger/store.py` 委托给后端**

顶部加导入：

```python
from src.ledger.backend import LedgerBackend, LocalFileLedgerBackend
```

`LedgerStore` 改为：

```python
class LedgerStore:
    """报告生成台账。只增不改。持久化委托给可插拔后端（默认本地文件）。"""

    def __init__(
        self, path: Path = DEFAULT_LEDGER_DIR, *, backend: LedgerBackend | None = None
    ) -> None:
        self.path = path  # 保留：既有调用点/测试仍读它
        self._backend: LedgerBackend = backend or LocalFileLedgerBackend(path)

    def append(self, entry: LedgerEntry) -> str:
        self._校验记录号(entry.记录号)
        self._backend.append(entry.记录号, entry.生成时间, to_dict(entry))
        logger.info("记台账 %s（报告 %s，经手人 %s）", entry.记录号, entry.报告编号, entry.经手人)
        return entry.记录号

    def list_all(self) -> tuple[LedgerEntry, ...]:
        entries = self._读全部()
        entries.sort(key=lambda e: (e.生成时间, e.记录号), reverse=True)
        return tuple(entries)

    def get(self, 记录号: str) -> LedgerEntry | None:
        # 仍是"读全部再按内容里的记录号过滤"——不拿记录号拼路径，防路径穿越。
        return next((e for e in self._读全部() if e.记录号 == 记录号), None)

    @staticmethod
    def _校验记录号(记录号: str) -> None:
        if (
            not 记录号
            or "/" in 记录号
            or "\\" in 记录号
            or "\0" in 记录号
            or 记录号 in (".", "..")
        ):
            logger.warning("记录号形状不安全，拒绝落台账：%r", 记录号)
            raise ValueError(f"记录号不合法：{记录号!r}")

    def _读全部(self) -> list[LedgerEntry]:
        entries: list[LedgerEntry] = []
        for payload in self._backend.iter_payloads():
            try:
                entries.append(from_dict(payload))
            except (KeyError, TypeError, ValueError):
                logger.warning("台账 payload 还原失败，已跳过", exc_info=True)
        return entries
```

删掉原来搬进后端的 import（若 `os`/`json` 在 store.py 再无它用则移除，避免 ruff 报未用导入）。

- [ ] **Step 3: 跑既有台账测试确认行为不变**

Run: `uv run pytest tests/test_ledger_store.py tests/test_ledger_replay.py tests/test_web_ledger.py -q`
Expected: 全 PASS（文件名格式、原子写、坏文件跳过、路径穿越拒绝、replay 复现——一字未变）。

- [ ] **Step 4: 类型 + 风格**

Run: `uv run mypy src/ledger/ && uv run ruff check src/ledger/`
Expected: 干净。

- [ ] **Step 5: Commit**

```bash
git add src/ledger/backend.py src/ledger/store.py
git commit -m "refactor(ledger): 抽出 LedgerBackend 协议 + 本地文件后端（行为不变）"
```

---

### Task 2: InMemoryLedgerBackend + 契约测试

**Files:**
- Modify: `src/ledger/backend.py`
- Test: `tests/test_ledger_backend.py`

**Interfaces:**
- Consumes: `LedgerBackend`（Task 1）。
- Produces: `InMemoryLedgerBackend()`：内存实现，供测试/离线；append-only。

- [ ] **Step 1: 写失败测试 `tests/test_ledger_backend.py`**

```python
"""台账后端契约：LedgerStore 跑在任意后端上、内存后端往返一致、append-only。"""

from datetime import datetime

import pytest

from src.ledger.backend import InMemoryLedgerBackend
from src.ledger.model import LedgerEntry
from src.ledger.store import LedgerStore
from tests.test_ledger_model import make_entry  # 既有构造助手；若无则见 Step 1a


def test_inmemory_roundtrip() -> None:
    backend = InMemoryLedgerBackend()
    backend.append("abc123", datetime(2026, 5, 1, 9, 0, 0), {"记录号": "abc123", "x": 1})
    payloads = list(backend.iter_payloads())
    assert payloads == [{"记录号": "abc123", "x": 1}]


def test_store_over_inmemory_append_list_get() -> None:
    store = LedgerStore(backend=InMemoryLedgerBackend())
    entry = make_entry(记录号="rec001")
    assert store.append(entry) == "rec001"
    assert [e.记录号 for e in store.list_all()] == ["rec001"]
    assert store.get("rec001") is not None
    assert store.get("不存在") is None


def test_backend_has_no_mutation_api() -> None:
    """铁律 #4：后端不得有 update/delete。"""
    backend = InMemoryLedgerBackend()
    assert not hasattr(backend, "update")
    assert not hasattr(backend, "remove")
    assert not hasattr(backend, "delete")
```

> Step 1a（仅当 `tests/test_ledger_model.py` 没有可复用的 `make_entry`）：在本文件内就地构造一个最小 `LedgerEntry`，字段照 `src/ledger/model.py` 的 `LedgerEntry` 定义填齐；实现时以该文件为准，不猜字段。

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/test_ledger_backend.py -q`
Expected: FAIL（`InMemoryLedgerBackend` 不存在）。

- [ ] **Step 3: 在 `src/ledger/backend.py` 加内存后端**

```python
class InMemoryLedgerBackend:
    """内存台账后端：供测试与离线。append-only，不落盘。"""

    def __init__(self) -> None:
        self._payloads: list[dict[str, object]] = []

    def append(self, record_id: str, created_at: datetime, payload: dict[str, object]) -> None:
        self._payloads.append(payload)

    def iter_payloads(self) -> Iterator[dict[str, object]]:
        return iter(list(self._payloads))
```

`__all__` 追加 `"InMemoryLedgerBackend"`。

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run pytest tests/test_ledger_backend.py -q`
Expected: PASS。

- [ ] **Step 5: 全量回归 + 类型 + 风格**

Run: `uv run pytest -q && uv run mypy src/ && uv run ruff check .`
Expected: 全绿（原 487 + 新增，无回归）。

- [ ] **Step 6: Commit**

```bash
git add src/ledger/backend.py tests/test_ledger_backend.py
git commit -m "feat(ledger): 内存台账后端 + 后端契约测试（append-only）"
```

---

## Self-Review

**Spec coverage**：本刀只认领"LedgerStore 可插拔后端"——Task 1 抽协议+本地后端（行为不变），Task 2 内存后端+契约测试。承接钉钉设计 §5「Store 加可切换后端」的第一步。✅

**Placeholder 扫描**：无 TBD。Step 1a 是"以既有文件为准"的明确指示（构造助手可能已存在），不是占位。backend.py Step 1 里标注了要删的笔误 `//`。

**类型一致**：`LedgerBackend.append(record_id, created_at, payload)` / `iter_payloads()` 在 Protocol、LocalFile、InMemory、LedgerStore 四处签名一致；`LedgerStore(path, *, backend=None)` 保留 `path` 属性与默认本地后端，既有调用点零改。

**范围**：只 LedgerStore，不碰其余 3 个 Store、不做权限/同步/鉴权（那些等真后端 + 身份）。行为保持由既有台账测试全绿护住。

## 后续（不在本刀）

- 其余 3 个 Store 各自一刀（InstanceStore 整档 load/save 形状不同，改动更需当心）。
- **真远端后端 = 宜搭 OpenAPI 适配器**（承载层已定：宜搭，不自建后端，见 design §2b）——`LedgerBackend` 多一个"宜搭"实现（append=新增一行、iter_payloads=读表），本刀的内存后端即它要满足的**可执行契约**。
- 权限裁剪下发、append-only 同步协议、鉴权/会话令牌：配合真后端 + 用户身份做。
