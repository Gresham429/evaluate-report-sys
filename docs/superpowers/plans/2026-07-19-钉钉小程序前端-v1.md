# 钉钉小程序前端 v1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 钉钉原生小程序：估价师现场手机填 19 项基本信息 + 地图预填地理事实、免登记填报人、暂存/续填/提交进多维表；办公端（现成）拉「已提交」预填出报告。

**Architecture:** 小程序（前端，不持密钥）→ 已上线的 serverless broker（`saveDraft/loadDraft/submit/prefillGeo` + 本计划新增 `whoami`）→ 钉钉多维表。broker 是唯一持密钥、换 token 的地方。

**Tech Stack:** 钉钉原生小程序（axml/acss/js/json，`dd.*` API）；broker 侧 Python 3.11+ 标准库。

## Global Constraints
- 设计依据：`docs/superpowers/specs/2026-07-19-钉钉小程序前端-design.md`。**v1 只做**：19 基本信息 + 地图预填 + 免登 + 暂存/续填(本机)/提交。**不做**：28 档次、逐因素描述、照片（v2）。
- content 形状必须与办公端可读一致：`{basic:{<19 SURVEY_FIELDS 键>}, gps:{lat,lng}, subjects:[], subject_levels:{}, asset_conditions:{}, photos:[]}`。v1 只填 `basic`+`gps`，其余空。
- broker 行契约、状态值（草稿/已提交）不改；新增 `whoami` 不动现有四接口。
- broker 侧：Python stdlib only、ruff line-length 100、全类型注解、`__all__`、无裸 except；`uv run` 跑测试/ruff/mypy。
- 小程序侧无自动化测试环境（须钉钉开发者工具真机），故小程序任务的"测试"是**人工真机核对**，代码须完整可上传。
- broker BASE_URL：`https://zhenghee-report-bmpupipvct.cn-hangzhou.fcapp.run`（当前 FC HTTP 触发器，anonymous）。
- 19 基本字段键（`src/extractor/field_map.py::SURVEY_FIELDS`）与中文标签：
  project_name 项目名称 / client 委托人 / client_address 委托人地址 / legal_rep 法定代表人 / purpose 估价目的 / survey_date 实地查勘日期 / value_date 价值时点 / materials 提供资料 / certificate_status 权属证书情况 / owner 权利人 / address 坐落 / usage 设定出租用途 / scale 规模 / scope 估价范围 / current_status 利用现状 / surveyor 查勘人 / report_no 报告编号(可空,办公端领) / issue_date 报告出具日期 / work_period 估价作业期。
- 类别下拉值（`src/model.py::Category`）：农用 / 办公 / 商业 / 住宅 / 工业 / 停车场用地 / 建设用地。

---

## File Structure

```
serverless/survey_broker/
├── identity.py            # 新增：DingtalkIdentity —— authCode → {userid,name}（免登）
├── handler.py             # 改：dispatch 增 whoami 路由 + identity 协作者
├── server.py              # 改：build_context 多建一个 identity
src/dingtalk/notable.py    # 改：暴露 public access_token()（供 identity 复用 token）
tests/test_survey_broker_identity.py   # 新增
tests/test_survey_broker_handler.py    # 改：加 whoami 路由测试

miniprogram/               # 钉钉小程序根（钉钉开发者工具打开/上传）
├── app.json               # 页面注册 + window
├── app.js                 # 全局：BASE_URL、filler 缓存
├── app.acss               # 全局样式
├── utils/broker.js        # POST broker 封装
├── pages/index/index.{axml,acss,js,json}   # 入口：免登+草稿列表+新建
└── pages/form/form.{axml,acss,js,json}      # 表单：19字段+地图预填+暂存/提交
```

---

## Task 1: broker `whoami`（authCode → userid，Python，TDD）

**Files:**
- Modify: `src/dingtalk/notable.py`（加 public `access_token()`）
- Create: `serverless/survey_broker/identity.py`
- Modify: `serverless/survey_broker/handler.py`
- Modify: `serverless/survey_broker/server.py`
- Create: `tests/test_survey_broker_identity.py`
- Modify: `tests/test_survey_broker_handler.py`

**Interfaces:**
- Consumes: `NotableClient`（现有，token 缓存）。
- Produces:
  - `NotableClient.access_token() -> str`（public，薄包 `_access_token`）。
  - `identity.DingtalkIdentity(token_provider: Callable[[], str], transport: IdentityTransport | None)`；`whoami(auth_code: str) -> dict[str, str]` 返回 `{"userid":..., "name":...}`。
  - `dispatch` 新增 `whoami` 路由：payload `{authCode}` → `identity.whoami(authCode)`。

- [ ] **Step 1: 暴露 NotableClient.access_token()**

`src/dingtalk/notable.py`，在 `_access_token` 之后加：

```python
    def access_token(self) -> str:
        """公开取当前 accessToken（带缓存）。供同应用的免登等其它调用复用同一 token。"""
        return self._access_token()
```

- [ ] **Step 2: 写 identity 失败测试**

`tests/test_survey_broker_identity.py`：

```python
"""DingtalkIdentity.whoami：authCode → userid。注入假 token_provider + 假 transport。"""

import json

import pytest

from serverless.survey_broker.identity import DingtalkIdentity


def _transport_ok(_method: str, _url: str, _headers: dict, _body: bytes | None) -> tuple[int, str]:
    return 200, json.dumps({"errcode": 0, "result": {"userid": "u-42", "name": "张三"}})


def test_whoami_returns_userid_and_name() -> None:
    idy = DingtalkIdentity(lambda: "tok", transport=_transport_ok)
    assert idy.whoami("code-abc") == {"userid": "u-42", "name": "张三"}


def test_whoami_bad_code_raises_valueerror() -> None:
    def _transport_bad(_m: str, _u: str, _h: dict, _b: bytes | None) -> tuple[int, str]:
        return 200, json.dumps({"errcode": 40078, "errmsg": "invalid code"})

    idy = DingtalkIdentity(lambda: "tok", transport=_transport_bad)
    with pytest.raises(ValueError):
        idy.whoami("bad")


def test_whoami_http_error_raises_valueerror() -> None:
    def _transport_500(_m: str, _u: str, _h: dict, _b: bytes | None) -> tuple[int, str]:
        return 500, "oops"

    idy = DingtalkIdentity(lambda: "tok", transport=_transport_500)
    with pytest.raises(ValueError):
        idy.whoami("x")
```

- [ ] **Step 3: 跑测试确认失败**

Run: `cd 估价报告系统 && uv run python -m pytest tests/test_survey_broker_identity.py -q`
Expected: FAIL（`ModuleNotFoundError: identity`）

- [ ] **Step 4: 写 identity 实现**

`serverless/survey_broker/identity.py`：

```python
"""钉钉免登：authCode → userid（+ 姓名）。broker 用它取"填报人"身份。

token 复用同一企业内部应用的 accessToken（由 `token_provider` 注入，通常是
`NotableClient.access_token`）——免登和多维表读写是同一个应用，token 通用。
transport 可注入（同 NotableClient 的 Transport 契约），单测零网络。

**具体免登接口待真机校准**：现按「用 access_token 调 getuserinfo、body 带 code」
假定（候选 topapi/v2/user/getuserinfo）。部署后打一次真机，据真实响应改
`_URL` 与结果解析（result.userid / result.name）。
"""

import json
import logging
import urllib.error
import urllib.request
from collections.abc import Callable
from typing import Any

logger = logging.getLogger(__name__)

__all__ = ["DingtalkIdentity", "IdentityTransport"]

# 待真机校准：免登换 userid 的端点与请求形状
_URL = "https://oapi.dingtalk.com/topapi/v2/user/getuserinfo"

# 同 NotableClient.Transport：(method, url, headers, body) -> (status, text)
IdentityTransport = Callable[[str, str, dict[str, str], bytes | None], tuple[int, str]]


def _urllib_transport(method: str, url: str, headers: dict[str, str], body: bytes | None) -> tuple[int, str]:
    req = urllib.request.Request(url, data=body, method=method)
    for k, v in headers.items():
        req.add_header(k, v)
    try:
        with urllib.request.urlopen(req, timeout=15) as resp:  # noqa: S310  仅钉钉域名
            return resp.status, resp.read().decode("utf-8")
    except urllib.error.HTTPError as exc:
        return exc.code, exc.read().decode("utf-8", "replace")
    except urllib.error.URLError as exc:
        return 0, f"网络错误：{exc}"


class DingtalkIdentity:
    """钉钉免登换身份。"""

    def __init__(
        self, token_provider: Callable[[], str], *, transport: IdentityTransport | None = None
    ) -> None:
        self._token = token_provider
        self._transport = transport or _urllib_transport

    def whoami(self, auth_code: str) -> dict[str, str]:
        """authCode → {"userid":..., "name":...}。无效/失败一律 ValueError（映射成 4xx）。"""
        url = f"{_URL}?access_token={self._token()}"
        body = json.dumps({"code": auth_code}, ensure_ascii=False).encode("utf-8")
        status, text = self._transport("POST", url, {"Content-Type": "application/json"}, body)
        if status != 200:
            raise ValueError(f"免登接口 HTTP{status}：{text[:120]}")
        try:
            obj: dict[str, Any] = json.loads(text)
        except (json.JSONDecodeError, TypeError) as exc:
            raise ValueError(f"免登响应非 JSON：{exc}") from exc
        if obj.get("errcode"):
            raise ValueError(f"免登失败：{obj.get('errmsg') or obj.get('errcode')}")
        result = obj.get("result") or {}
        userid = str(result.get("userid") or "")
        if not userid:
            raise ValueError("免登响应缺 userid（接口形状待校准）")
        return {"userid": userid, "name": str(result.get("name") or "")}
```

- [ ] **Step 5: dispatch 加 whoami 路由**

`serverless/survey_broker/handler.py`：给 `_Amap` 旁加一个 `_Identity` Protocol，`dispatch` 增参 `identity` 与 `whoami` 分支。

顶部 Protocol 区加：

```python
class _Identity(Protocol):
    def whoami(self, auth_code: str) -> dict[str, Any]: ...
```

`dispatch` 签名与分支：

```python
def dispatch(
    action: str, payload: dict[str, Any], *, store: _Store, amap: _Amap, identity: _Identity
) -> tuple[int, dict[str, Any]]:
    if not isinstance(payload, dict):
        return 400, {"error": "payload 必须是对象"}
    try:
        if action == "whoami":
            return 200, identity.whoami(str(_require(payload, "authCode")))
        # …（saveDraft/loadDraft/submit/prefillGeo 原分支不变）…
```

`handler()` 内构造 identity 并传入 dispatch：

```python
    from serverless.survey_broker.identity import DingtalkIdentity  # 顶部 import 亦可
    identity = DingtalkIdentity(client.access_token)
    ...
    status, result = dispatch(action, payload, store=store, amap=amap, identity=identity)
```

- [ ] **Step 6: server.py build_context 带 identity**

`serverless/survey_broker/server.py`：`build_context` 返回改为 `(store, amap, identity)`，`main`/`handle` 相应传入。`handle` 签名加 `identity`，调用 `dispatch(..., identity=identity)`。

```python
def build_context() -> tuple[SurveyBrokerStore, AmapClient, DingtalkIdentity]:
    client = NotableClient(...)  # 同现有
    store = SurveyBrokerStore(client, os.environ["NOTABLE_SURVEY_SHEET"])
    amap = AmapClient(os.environ.get("AMAP_KEY", ""))
    identity = DingtalkIdentity(client.access_token)
    return store, amap, identity
```
`handle(body_bytes, *, store, amap, identity)` 内 `dispatch(..., identity=identity)`；`_make_handler`/`main` 透传。

- [ ] **Step 7: 改 handler 测试加 whoami，改 server 测试传 identity**

`tests/test_survey_broker_handler.py` 的 `FakeAmap` 旁加 `FakeIdentity`：

```python
class FakeIdentity:
    def whoami(self, auth_code: str) -> dict[str, object]:
        if auth_code == "good":
            return {"userid": "u1", "name": "张三"}
        raise ValueError("bad code")
```
所有 `dispatch(...)` 调用补 `identity=FakeIdentity()`。新增：

```python
def test_dispatch_whoami_ok() -> None:
    status, body = dispatch("whoami", {"authCode": "good"},
                            store=FakeStore(), amap=FakeAmap(), identity=FakeIdentity())
    assert status == 200
    assert body["userid"] == "u1"

def test_dispatch_whoami_bad_code_400() -> None:
    status, body = dispatch("whoami", {"authCode": "nope"},
                            store=FakeStore(), amap=FakeAmap(), identity=FakeIdentity())
    assert status == 400
```
`tests/test_survey_broker_server.py`：`handle(...)` 调用补 `identity=FakeIdentity()`（在该文件定义同款 FakeIdentity）。

- [ ] **Step 8: 跑测试确认通过**

Run: `cd 估价报告系统 && uv run python -m pytest tests/test_survey_broker_identity.py tests/test_survey_broker_handler.py tests/test_survey_broker_server.py -q`
Expected: PASS

- [ ] **Step 9: lint + 类型**

Run: `cd 估价报告系统 && uv run ruff check src/dingtalk/notable.py serverless/ tests/test_survey_broker_*.py && uv run mypy src/dingtalk/notable.py serverless/`
Expected: 无错误

- [ ] **Step 10: 重新打包 + 提交**

```bash
cd 估价报告系统
uv run python serverless/package.py   # identity.py 已在 FILES? 见下
git add src/dingtalk/notable.py serverless/survey_broker/identity.py serverless/survey_broker/handler.py serverless/survey_broker/server.py tests/test_survey_broker_identity.py tests/test_survey_broker_handler.py tests/test_survey_broker_server.py
git commit -m "feat(broker): whoami 免登(authCode→userid) + NotableClient.access_token"
```
> **注意**：`serverless/package.py` 的 `FILES` 列表须补 `serverless/survey_broker/identity.py`，否则打包漏文件。本步先改 package.py 再打包。

---

## Task 2: 小程序骨架 + broker 封装

**Files:** Create `miniprogram/app.json`、`app.js`、`app.acss`、`utils/broker.js`

**Interfaces:** Produces `app.globalData.BASE_URL`、`app.globalData.filler`；`broker.request(action, payload) -> Promise<obj>`。

- [ ] **Step 1: app.json**

```json
{
  "pages": ["pages/index/index", "pages/form/form"],
  "window": {
    "defaultTitle": "实勘问卷",
    "titleBarColor": "#1677ff",
    "pullRefresh": false
  }
}
```

- [ ] **Step 2: app.js**

```js
App({
  globalData: {
    BASE_URL: 'https://zhenghee-report-bmpupipvct.cn-hangzhou.fcapp.run',
    filler: '',      // 免登拿到的 userid（入口页写入）
    fillerName: '',
  },
});
```

- [ ] **Step 3: app.acss**

```css
page { background: #f5f6f8; color: #222; font-size: 30rpx; }
.btn-primary { background: #1677ff; color: #fff; border-radius: 8rpx; }
.card { background: #fff; margin: 16rpx; padding: 24rpx; border-radius: 12rpx; }
.muted { color: #888; font-size: 26rpx; }
```

- [ ] **Step 4: utils/broker.js**

```js
const app = getApp();

/** POST {action, payload} 到 broker，返回解析后的对象；非 2xx 抛 {status, detail}。 */
function request(action, payload) {
  return new Promise((resolve, reject) => {
    dd.httpRequest({
      url: getApp().globalData.BASE_URL + '/',
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      data: JSON.stringify({ action, payload: payload || {} }),
      dataType: 'json',
      timeout: 20000,
      success: (res) => {
        if (res.status >= 200 && res.status < 300) resolve(res.data);
        else reject({ status: res.status, detail: (res.data && res.data.error) || '请求失败' });
      },
      fail: (err) => reject({ status: 0, detail: (err && err.errorMessage) || '网络错误' }),
    });
  });
}

module.exports = { request };
```

- [ ] **Step 5: 人工核对**

用钉钉开发者工具打开 `miniprogram/`，能编译、无红错、两页能空跑（下两任务补页面后）。**提交**：

```bash
cd 估价报告系统
git add miniprogram/app.json miniprogram/app.js miniprogram/app.acss miniprogram/utils/broker.js
git commit -m "feat(miniapp): 骨架 app.json/js/acss + broker 请求封装"
```

---

## Task 3: 入口页（免登 + 我的草稿 + 新建）

**Files:** Create `miniprogram/pages/index/index.{axml,acss,js,json}`

**Interfaces:** Consumes `broker.request('whoami'|'loadDraft')`；本机 `dd.getStorage('myDrafts')` 存本机存过的 survey_id 列表。Produces：跳 `pages/form?draftId=<id>` 续填 / `pages/form`（新建）。

- [ ] **Step 1: index.json**

```json
{ "defaultTitle": "实勘问卷" }
```

- [ ] **Step 2: index.axml**

```xml
<view class="card">
  <view>填报人：{{fillerName || filler || '（免登中…）'}}</view>
  <view class="muted" a:if="{{authError}}">免登失败：{{authError}}（可先新建，提交前重试）</view>
</view>

<view class="card">
  <button class="btn-primary" onTap="onNew">新建实勘问卷</button>
</view>

<view class="card" a:if="{{drafts.length > 0}}">
  <view>本机草稿（可续填）</view>
  <view class="draft" a:for="{{drafts}}" key="{{item.survey_id}}" data-id="{{item.survey_id}}" onTap="onResume">
    <text>{{item.category}} · {{item.updated_at}}</text>
    <text class="muted">  {{item.survey_id}}</text>
  </view>
</view>
<view class="card muted" a:else>本机还没有草稿。</view>
```

- [ ] **Step 3: index.acss**

```css
.draft { padding: 16rpx 0; border-bottom: 1rpx solid #eee; }
```

- [ ] **Step 4: index.js**

```js
const broker = require('../../utils/broker');
const app = getApp();

Page({
  data: { filler: '', fillerName: '', authError: '', drafts: [] },

  onLoad() { this.auth(); },
  onShow() { this.loadMyDrafts(); },

  auth() {
    dd.getAuthCode({
      success: (r) => {
        broker.request('whoami', { authCode: r.authCode })
          .then((who) => {
            app.globalData.filler = who.userid;
            app.globalData.fillerName = who.name || '';
            this.setData({ filler: who.userid, fillerName: who.name || '' });
          })
          .catch((e) => this.setData({ authError: e.detail || '未知' }));
      },
      fail: (e) => this.setData({ authError: (e && e.errorMessage) || '取 authCode 失败' }),
    });
  },

  loadMyDrafts() {
    dd.getStorage({
      key: 'myDrafts',
      success: (res) => {
        const ids = res.data || [];
        Promise.all(ids.map((id) =>
          broker.request('loadDraft', { survey_id: id })
            .then((d) => ({ survey_id: id, category: d.category, updated_at: d.updated_at }))
            .catch(() => null)
        )).then((rows) => this.setData({ drafts: rows.filter(Boolean) }));
      },
      fail: () => this.setData({ drafts: [] }),
    });
  },

  onNew() { dd.navigateTo({ url: '/pages/form/form' }); },
  onResume(e) { dd.navigateTo({ url: '/pages/form/form?draftId=' + e.currentTarget.dataset.id }); },
});
```

- [ ] **Step 5: 人工核对 + 提交**

真机：入口页显示填报人（免登通）；有草稿则列出。**提交**：

```bash
cd 估价报告系统
git add miniprogram/pages/index/
git commit -m "feat(miniapp): 入口页 免登+本机草稿列表+新建"
```

---

## Task 4: 表单页（19 字段 + 地图预填 + 暂存/提交）

**Files:** Create `miniprogram/pages/form/form.{axml,acss,js,json}`

**Interfaces:** Consumes `broker.request('saveDraft'|'submit'|'prefillGeo'|'loadDraft')`、`dd.getLocation`。Produces：写多维表（草稿/已提交）+ 本机 `myDrafts` 记 survey_id。

- [ ] **Step 1: form.json**

```json
{ "defaultTitle": "填写实勘问卷" }
```

- [ ] **Step 2: form.axml**

```xml
<view class="card">
  <view class="row">
    <text class="label">类别</text>
    <picker onChange="onCategory" data-key="category" range="{{categories}}" value="{{catIndex}}">
      <view class="pick">{{form.category || '请选择'}}</view>
    </picker>
  </view>
</view>

<view class="card">
  <view class="row" a:for="{{fields}}" key="{{item.key}}">
    <text class="label">{{item.label}}</text>
    <input class="inp" value="{{form.basic[item.key]}}" data-key="{{item.key}}" onInput="onField" placeholder="填写" />
  </view>
</view>

<view class="card">
  <button class="btn-primary" onTap="onGeo">地图预填地理事实</button>
  <view a:if="{{geo.address}}">
    <view>地址：{{geo.address}}</view>
    <view class="muted">公交：{{geo.bus_stops}}</view>
    <view class="muted">最近地铁：{{geo.metroText}}</view>
    <view class="muted">周边：{{geo.facilities}}</view>
  </view>
  <view class="muted">仅供参考，档次由估价师判断（本版不采档次）。</view>
</view>

<view class="card">
  <button onTap="onSave">暂存</button>
  <button class="btn-primary" onTap="onSubmit">提交</button>
  <view class="muted" a:if="{{msg}}">{{msg}}</view>
</view>
```

- [ ] **Step 3: form.acss**

```css
.row { display: flex; align-items: center; padding: 12rpx 0; }
.label { width: 200rpx; color: #555; }
.inp, .pick { flex: 1; border: 1rpx solid #ddd; border-radius: 6rpx; padding: 10rpx; }
```

- [ ] **Step 4: form.js**

```js
const broker = require('../../utils/broker');
const app = getApp();

const FIELDS = [
  { key: 'project_name', label: '项目名称' }, { key: 'client', label: '委托人' },
  { key: 'client_address', label: '委托人地址' }, { key: 'legal_rep', label: '法定代表人' },
  { key: 'purpose', label: '估价目的' }, { key: 'survey_date', label: '实地查勘日期' },
  { key: 'value_date', label: '价值时点' }, { key: 'materials', label: '提供资料' },
  { key: 'certificate_status', label: '权属证书情况' }, { key: 'owner', label: '权利人' },
  { key: 'address', label: '坐落' }, { key: 'usage', label: '设定出租用途' },
  { key: 'scale', label: '规模' }, { key: 'scope', label: '估价范围' },
  { key: 'current_status', label: '利用现状' }, { key: 'surveyor', label: '查勘人' },
  { key: 'report_no', label: '报告编号(可空)' }, { key: 'issue_date', label: '报告出具日期' },
  { key: 'work_period', label: '估价作业期' },
];
const CATEGORIES = ['农用', '办公', '商业', '住宅', '工业', '停车场用地', '建设用地'];

Page({
  data: {
    fields: FIELDS, categories: CATEGORIES, catIndex: 0,
    form: { category: '', basic: {} },
    survey_id: '', gps: null, geo: {}, msg: '',
  },

  onLoad(q) {
    if (q && q.draftId) this.resume(q.draftId);
  },

  resume(id) {
    broker.request('loadDraft', { survey_id: id }).then((d) => {
      const c = d.content || {};
      this.setData({
        survey_id: id,
        form: { category: d.category || '', basic: c.basic || {} },
        gps: c.gps || null,
        catIndex: Math.max(0, CATEGORIES.indexOf(d.category)),
      });
    }).catch((e) => this.setData({ msg: '载入草稿失败：' + e.detail }));
  },

  onCategory(e) {
    const cat = CATEGORIES[e.detail.value];
    this.setData({ catIndex: e.detail.value, 'form.category': cat });
  },
  onField(e) {
    this.setData({ ['form.basic.' + e.currentTarget.dataset.key]: e.detail.value });
  },

  onGeo() {
    dd.getLocation({
      success: (loc) => {
        this.setData({ gps: { lat: loc.latitude, lng: loc.longitude } });
        broker.request('prefillGeo', { lng: loc.longitude, lat: loc.latitude }).then((f) => {
          const metro = f.nearest_metro;
          this.setData({ geo: {
            address: f.address,
            bus_stops: (f.bus_stops || []).join('、') || '（无）',
            facilities: (f.facilities || []).slice(0, 6).join('、'),
            metroText: metro ? (metro.name + ' 约' + metro.distance_m + '米') : '（无）',
          }});
        }).catch((e) => this.setData({ msg: '地图预填失败：' + e.detail }));
      },
      fail: (e) => this.setData({ msg: '取定位失败：' + ((e && e.errorMessage) || '') }),
    });
  },

  _content() {
    return {
      basic: this.data.form.basic,
      gps: this.data.gps,
      subjects: [], subject_levels: {}, asset_conditions: {}, photos: [],
    };
  },
  _payload() {
    return {
      survey_id: this.data.survey_id || undefined,
      filler: app.globalData.filler || '',
      category: this.data.form.category,
      updated_at: new Date().toISOString(),
      content: this._content(),
    };
  },

  onSave() {
    if (!this.data.form.category) { this.setData({ msg: '请先选类别' }); return; }
    broker.request('saveDraft', this._payload()).then((r) => {
      this.rememberLocal(r.survey_id);
      this.setData({ survey_id: r.survey_id, msg: '已暂存：' + r.survey_id });
    }).catch((e) => this.setData({ msg: '暂存失败：' + e.detail }));
  },

  onSubmit() {
    if (!this.data.form.category) { this.setData({ msg: '请先选类别' }); return; }
    // 先存（拿到/更新 survey_id）再提交
    broker.request('saveDraft', this._payload()).then((r) => {
      this.rememberLocal(r.survey_id);
      this.setData({ survey_id: r.survey_id });
      return broker.request('submit', { survey_id: r.survey_id });
    }).then(() => {
      this.setData({ msg: '已提交，办公端可拉取。' });
    }).catch((e) => this.setData({ msg: '提交失败：' + e.detail }));
  },

  rememberLocal(id) {
    dd.getStorage({ key: 'myDrafts', success: (res) => {
      const ids = res.data || [];
      if (ids.indexOf(id) < 0) ids.push(id);
      dd.setStorage({ key: 'myDrafts', data: ids });
    }, fail: () => dd.setStorage({ key: 'myDrafts', data: [id] }) });
  },
});
```

- [ ] **Step 5: 人工真机核对 + 提交**

真机全链路：新建→选类别→填字段→地图预填→暂存(得 survey_id)→回入口见草稿→续填→提交→**办公端拉取见这份**。**提交**：

```bash
cd 估价报告系统
git add miniprogram/pages/form/
git commit -m "feat(miniapp): 表单页 19字段+地图预填+暂存/提交"
```

---

## Task 5: 部署 + 集成真机门（人工）

- [ ] **Step 1: broker 重部署（含 whoami）** — 重跑 `uv run python serverless/package.py`，FC 重新上传 zip，`whoami` 真机校准（打一次 `{"action":"whoami","payload":{"authCode":"<真authCode>"}}` 看是否回 userid；若接口形状不符，改 `identity.py::_URL` 与解析，重打包重传）。
- [ ] **Step 2: 小程序域名白名单** — 钉钉开发者后台 → 小程序 → 服务器域名，加 `https://zhenghee-report-bmpupipvct.cn-hangzhou.fcapp.run`。
- [ ] **Step 3: 上传体验版** — 钉钉开发者工具上传 `miniprogram/`，组织内体验，真人走：免登→填→地图→暂存→续填→提交。
- [ ] **Step 4: 端到端** — 办公端（`.env` 加 `NOTABLE_SURVEY_SHEET=ePwqDH2` + `承载后端=多维表`）「从实勘问卷拉取」应看到真机提交那份、预填出表单。

---

## Self-Review
- **Spec 覆盖**：免登(Task1 whoami + Task3 auth)/基本信息19(Task4 FIELDS)/地图预填(Task4 onGeo+已上线 prefillGeo)/暂存续填(Task3+4 saveDraft/loadDraft/本机 myDrafts)/提交(Task4 submit) 全覆盖；档次/描述/照片明确不在本计划（v2）。
- **Placeholder**：各步含完整代码（axml/js/py 全给）。
- **契约一致**：content 六键（basic/gps/subjects/subject_levels/asset_conditions/photos）与 broker `record.py`、办公 `_fields_to_response` 一致；v1 只填 basic+gps。
- **待校准**：`identity.py` 免登端点 `# 待真机校准`（Task5 Step1 核）。
- **依赖顺序**：Task1(broker whoami)→Task2(骨架)→Task3(入口,需 whoami/loadDraft)→Task4(表单,需 saveDraft/submit/prefillGeo)→Task5(部署真机)。
